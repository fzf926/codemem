#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd -P)"
cd "$ROOT"
if [ -x "$ROOT/core/dist/codemem-agent" ]; then
  "$ROOT/core/dist/codemem-agent" "$@" --root "$ROOT"
else
  bun run core/src/cli/agent.ts "$@" --root "$ROOT"
fi
