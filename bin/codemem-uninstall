#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd -P)"
cd "$ROOT"
if [ -x "$ROOT/core/dist/codemem-uninstall" ]; then
  "$ROOT/core/dist/codemem-uninstall" "$@" --root "$ROOT"
else
  bun run core/src/cli/uninstall.ts "$@" --root "$ROOT"
fi
