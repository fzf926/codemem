#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd -P)"
cd "$ROOT"
if [ -x "$ROOT/core/dist/codemem-install" ]; then
  "$ROOT/core/dist/codemem-install" "$@" --root "$ROOT"
else
  bun run core/src/cli/install.ts "$@" --root "$ROOT"
fi
