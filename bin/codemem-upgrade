#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd -P)"
cd "$ROOT"
if [ -x "$ROOT/core/dist/codemem-upgrade" ]; then
  "$ROOT/core/dist/codemem-upgrade" "$@" --root "$ROOT"
else
  bun run core/src/cli/upgrade.ts "$@" --root "$ROOT"
fi
