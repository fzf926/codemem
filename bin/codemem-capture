#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd -P)"
cd "$ROOT"
if [ -x "$ROOT/core/dist/codemem-capture" ]; then
  "$ROOT/core/dist/codemem-capture" "$@" --root "$ROOT"
else
  bun run core/src/cli/capture.ts "$@" --root "$ROOT"
fi
