#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd -P)"
cd "$ROOT"
if [ -x "$ROOT/core/dist/codemem-init" ]; then
  "$ROOT/core/dist/codemem-init" "$@" --root "$ROOT"
else
  bun run core/src/cli/init.ts "$@" --root "$ROOT"
fi
