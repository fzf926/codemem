#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd -P)"
cd "$ROOT"
if [ -x "$ROOT/core/dist/codemem-build" ]; then
  "$ROOT/core/dist/codemem-build" "$@" --root "$ROOT"
else
  bun run core/src/cli/build.ts "$@" --root "$ROOT"
fi
