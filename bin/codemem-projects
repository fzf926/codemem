#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd -P)"
cd "$ROOT"
if [ -x "$ROOT/core/dist/codemem-projects" ]; then
  "$ROOT/core/dist/codemem-projects" "$@" --root "$ROOT"
else
  bun run core/src/cli/projects.ts "$@" --root "$ROOT"
fi
